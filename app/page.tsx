export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-6 px-6">
      <p className="text-sm tracking-[0.3em] text-sky-400">THE SEALED ORACLE</p>
      <h1 className="text-5xl font-semibold">EVA</h1>
      <p className="text-lg text-zinc-300">
        She seals every World Cup prediction on-chain before kickoff — proof she
        can&apos;t rewrite history — and she gets sharper every match, because she
        remembers what she got wrong.
      </p>
      <p className="text-sm text-emerald-400">
        Memory is the seedling: proof that what was learned persists.
      </p>
      <div className="mt-4 text-xs text-zinc-500">
        Wiring in progress — the oracle wakes when her memory and match feed are
        connected.
      </div>
    </main>
  );
}
